# Model artifacts

Downloaded base weights, adapters, merged checkpoints, and quantized model files
belong under this directory during local development but are excluded from
ordinary Git. Record each artifact's source model, license, revision, training
manifest, evaluation result, and SHA-256 hash before registering it with the
agent.
