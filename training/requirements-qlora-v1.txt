# Reference environment: Python 3.11, Linux, CUDA 12.4-compatible driver.
# Install a CUDA-compatible PyTorch wheel separately before this file.
accelerate==1.8.1
bitsandbytes==0.46.1
datasets==3.6.0
peft==0.16.0
safetensors==0.5.3
# 5.5.0 is the first release that clears every advisory known to pip-audit at
# the 2026-08-10 security review. Revalidate PEFT/TRL APIs before a GPU run.
transformers==5.5.0
trl==0.19.1
